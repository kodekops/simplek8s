apiVersion: v1
kind: Pod
metadata:
  name: client-pod
  labels:
    component: client
spec:
  containers:
    - name: client
      image: kodega2016/multi-client
      ports:
        - containerPort: 3000
